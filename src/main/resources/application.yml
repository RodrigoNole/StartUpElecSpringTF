server:
    port: 8080
spring:
    datasource:
        driver-class-name: org.postgresql.Driver
        password: marina19
        url: jdbc:postgresql://localhost:5432/StartUpElecSpringTFDS
        username: postgres
    jpa:
        database: postgresql
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect
        show-sql: true
